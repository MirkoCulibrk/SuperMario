//making vector class for calculating position of all sprites
export class Vector{
    constructor(x,y){
        this.set(x,y);
    }
    //adding new
    set(x,y){
        this.x=x;
        this.y=y
    }
}